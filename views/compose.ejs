
<%- include("partials/header"); -%>
    <h1 class="mt-5 mb-3"><%= listTitle  %></h1>
    <!-- <% if (locals.newListItems) {%>
      <% newListItems.forEach(function(newListItems){ %>
        <ul>
          <li>
            <h3><%= newListItems.title %></h3>
            <% if (newListItems.content)  {%>
              <p><%= newListItems.content.slice(0, 30) %></p>
            <% } %>
          </li>
        </ul>
      <% }) %>
    <%}%> -->
    <form class="" action="/compose" method="post">
      <div class="form-group mt-5">
        <label>Title</label>
        <input class="form-control" type="text" name="postTitle">
        <label>Post</label>
        
        <div id="editor" name="postBody" >
          
        </div>
        <input id="postBody" type="hidden" name="postBody">
      </div>
      <button class="btn btn-primary" type="submit" name="button" onclick="logHtmlContent()">Publish</button>
    </form>
<%- include("partials/footer"); -%>
